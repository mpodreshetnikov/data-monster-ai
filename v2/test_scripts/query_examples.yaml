examples:
  - question: "Сколько было чеков каждой аптеке?"
    name: "example1"
    response: "SELECT ucsd.owner_key, COUNT(*) AS c FROM uas_cash_cheque ucc JOIN uas_cash_session_doc ucsd ON ucsd.session_id = ucc.cash_session_id GROUP BY ucsd.owner_key ORDER BY c DESC;"
  - question: "Сколько было чеков в каждой аптеке?"
    name: "example2"
    response: "SELECT ucsd.owner_key, COUNT(*) AS c FROM uas_cash_cheque ucc JOIN uas_cash_session_doc ucsd ON ucsd.session_id = ucc.cash_session_id GROUP BY ucsd.owner_key ORDER BY c DESC;"
  - question: "Сколько было чеков в каждой аптеке?"
    name: "example3"
    response: "SELECT ucsd.owner_key, COUNT(*) AS c FROM uas_cash_cheque ucc JOIN uas_cash_session_doc ucsd ON ucsd.session_id = ucc.cash_session_id GROUP BY ucsd.owner_key ORDER BY c DESC;"
